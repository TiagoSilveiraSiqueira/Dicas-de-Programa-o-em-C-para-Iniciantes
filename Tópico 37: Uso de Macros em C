#include <stdio.h>

#define MAX(x, y) ((x) > (y) ? (x) : (y))

int main() {
    int a = 5, b = 8;
    int maximo = MAX(a, b);

    printf("O máximo entre %d e %d é %d\n", a, b, maximo);

    return 0;
}
