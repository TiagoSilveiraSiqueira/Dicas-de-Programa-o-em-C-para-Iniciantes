#include <stdio.h>
#include <string.h>

int main() {
    char string1[] = "Hello";
    char string2[20];

    strcpy(string2, " World");
    strcat(string1, string2);

    printf("Concatenated string: %s\n", string1);
    printf("Length of string1: %lu\n", strlen(string1));

    return 0;
}
